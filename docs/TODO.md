# TODO 一覧 — 2026-09-04 時点

**熊田さんの作業**と**開発側の残作業**を分けて、優先度順に並べる。
この表が正本。終わったものは打ち消し線ではなく行を削除し、下の「完了」に1行で残す。

---

## A. 熊田さんの作業（順番どおりが最短）

### A2. 🔴 iOS: TestFlight のクラッシュを見る
https://appstoreconnect.apple.com/apps → BodyLoger → TestFlight → **フィードバック → クラッシュ**
- **KishimotoYumi さんの端末で 1.1.0 (81) がクラッシュ2件**・フィードバック3件が届いている
- スタックトレースを Claude に貼る。**Android の起動クラッシュと同根の可能性**があるので優先度が高い
- 同じ画面の **フィードバック → スクリーンショット** も3件確認

### A3. 🟠 Android: 起動クラッシュの検証

> ⚠️ **.aab は端末に直接インストールできない**（Play が端末ごとの apk に分割配信する形式）。
> ビルドしただけでは端末は古いままなので、**必ず 4〜5 で入れ替わりを確認してから**起動テストする。
> 2026-09-04 にこれを飛ばして1周無駄にした。
>
> **A11 のサービスアカウントを登録すると 2〜3 は自動になる**（ビルド成功で内部テストに入る）。
> 登録前は 2〜3 も手で行う。

1. Codemagic で **rn-android** を実行（https://codemagic.io/apps・main の最新）
   - 起動を止めない構造（safeBoot）と Intl 依存の排除が入っている
2. （A11未登録のあいだだけ）成功したビルドを開き **Artifacts → `app-release.aab`** をダウンロード
3. （同）https://play.google.com/console → BodyLoger → **テストとリリース → テスト → 内部テスト**
   → 「新しいリリースを作成」→ .aab をドラッグ → 保存 → リリースのレビュー → 公開を開始
   （処理に5〜15分。A11登録後はここまで自動）
4. 端末を更新する（**自動では降ってこない**）:
   Playストア → 右上のアイコン → **アプリとデバイスの管理 → 利用可能なアップデート** → 更新
5. **入れ替わったことを確認する**: 端末の 設定 → アプリ → BodyLog → アプリの詳細情報 で
   **versionCode が前回より大きい**こと。同じなら更新が届いていないので 4 に戻る
6. 起動するか確認
7. **落ちたら**: 設定画面に到達できないので A4（トレース取得）へ
8. **落ちたら（起動はできるが不安定）／落ちなくても**: 設定画面の最下部「起動時のエラー記録」を
   開いて**内容をコピー**して貼る（落ちなくても内部で捕捉している場合がある）
9. 一巡して Android 固有の崩れを見る（＋ボタンとジェスチャーバー・シート上端・
   戻るジェスチャー・通知・ヘルスケア行が出ていないこと・新パレット・ダーク）

### A4. 🟠 Android: リリース前レポートでトレースを取る（A3で落ちる場合）
https://play.google.com/console → BodyLoger → テストとリリース → **クローズドテスト – Alpha**
→ 新しいリリースを作成 → **「ライブラリから追加」で 1.1.0 (5) を選ぶ（再ビルド不要）**
→ 公開 → 30分〜数時間で **テスト → リリース前レポート → 安定性** にトレースが出る
- **内部テストではリリース前レポートは生成されない**（実機で確認済み）
- ただしクローズドテストの公開には下の A5（アプリのコンテンツ申告）が必要

### A5. 🔴 Play: アプリのコンテンツ申告（**Android の日程の律速**）
> **A12 の14日カウントはクローズドテストを公開してから始まる。**
> つまり **A5 を1日遅らせると Android の製品版公開が1日遅れる**。iOS の提出とは独立に、
> これだけは先に片付ける価値がある。

https://play.google.com/console → BodyLoger → ダッシュボードの「詳細」から順に
| 項目 | 回答 |
|---|---|
| 金融取引機能 | **含まれていない**（銀行・融資・仮想通貨・保険・投資・送金なし） |
| 健康 | 健康機能あり／**医療機器ではない**／調査研究でもない／記録・可視化・一般情報 |
| 写真と動画の権限 | **コア機能に必要**（食事写真からの栄養推定・体型記録）・継続的に必要 |
| 広告 | **広告が含まれる**（AdMob） |
| データセーフティ | **収集あり**（体重・食事・写真・ユーザーID）／暗号化して送信／削除リクエスト可／**トラッキングなし・第三者への販売なし** |
| アカウント削除URL | https://bodylog-orcin.vercel.app/delete-account |
| プライバシーポリシー | https://bodylog-orcin.vercel.app/privacy |
| コンテンツのレーティング | アンケート回答（全年齢想定） |
| 対象ユーザー | 18歳以上を推奨 |
| クローズドテストの国 | **日本** |
| ストアの掲載情報 | 簡単な説明・詳しい説明はチャット履歴の草案を貼る |

### A6. 🟡 iOS: ログイン自動入力の確認（設定は完了・再ビルド待ち）
**Apple Developer の設定と Codemagic の `ENABLE_ASSOCIATED_DOMAINS=true` は 2026-09-04 に完了。**
残っているのは `rn-testflight` を1回回して、実機でいつものアカウントが
パスワード候補に出ることを確認するだけ。
1. Apple Developer → App ID `com.gotcha.bodylog.rn` で **Associated Domains を ON**（**済み**）
2. **Profiles で「bodylog rn appstore 2」を削除** ← CI が自動で作り直すので実は不要になった
   （`codemagic.yaml` の「署名プロファイルの整合」ステップが古いプロファイルを捨てて作り直す）
3. Codemagic の環境変数グループ `rc` に **`ENABLE_ASSOCIATED_DOMAINS` = `true`** を追加
4. 再ビルド → いつものアカウントがパスワード候補に出るようになる
- Team ID `JBQJBFY3JJ` は AASA に埋め込み済み・本番配信済み（詳細 docs/LOGIN-AUTOFILL.md）

### A7. 🟡 テスター
- **彩羽根田さん が App Store Connect の招待を未承諾**（3日で期限切れ・切れていたら「招待を再送信」）
- 承諾後、TestFlight → 内部テスト「初期招待」グループへ追加
- 小澤祥太さんは追加済み（「招待済み」＝TestFlight アプリからインストール待ち）

### A9. 🟢 課金を点火するとき（3点セットは必ず同時）
1. `EXPO_PUBLIC_RC_IOS_KEY` / `_ANDROID_KEY` を設定
2. `AI_LIMITS_ENABLED=true`（Vercel）
3. 王冠の表示範囲を広げる
- **その前に `RC_WEBHOOK_SECRET` をローテーション**（スクショで漏洩済み・Vercel と RevenueCat の両方）
- 国=日本の Sandbox テスターで通貨が ¥ になることを確認（docs/RELEASE-RISKS.md A3/A4）

### A10. 🟢 バックアップ（PCとGoogleアカウントが無くなる前に）
`C:\Users\hashi\Documents\BodyLog-backup\` の4つを **gotcha429@gmail.com の Drive** へドラッグ
- `01_署名鍵_最重要`（キーストア・ASC APIキー・iOS証明書）← **失うと更新不能**
- `02_ローカルのみ設定`（.env.local）／`03_素材`（アイコン元画像）／`04_Claudeの記憶`
- `05_業務メモ_社内限` は BodyLog 無関係なので除く
- 詳細 docs/HANDOVER.md（コード・設計・SQL は GitHub にあるので失われない）

### A11. 🟠 Play への自動アップロード: 初回ビルドで疎通確認（設定は完了）
**サービスアカウント作成・Play での権限付与・Codemagic への登録はすべて 2026-09-04 に完了。**
残っているのは `rn-android` を1回回して、実際に内部テストへ入るかを見るだけ（A3 と同じ1回で兼ねられる）。

- ❌ `403` / `The caller does not have permission` が出たら**権限の反映待ち**（数分〜最大24時間）。
  設定ミスではないので時間を置いて再実行。その間は Artifacts から手動アップロードもできる

<details><summary>実施済みの手順（記録）</summary>


1. **サービスアカウント作成** https://console.cloud.google.com/iam-admin/serviceaccounts
   → 「サービス アカウントを作成」→ 名前 `codemagic-play-publisher` → 作成
   → **ロールは付けない** → 完了 → キー → 鍵を追加 → 新しい鍵を作成 → **JSON** をダウンロード
   → API を有効化 https://console.cloud.google.com/apis/library/androidpublisher.googleapis.com
2. **Play で権限付与** https://play.google.com/console → 左下 **ユーザーと権限** → 新しいユーザーを招待
   → サービスアカウントのメールアドレス → アプリの権限で BodyLoger を選び
   **「テスト版リリースを管理」＋「アプリ情報の閲覧」**だけ付ける（製品版の権限は与えない）
3. **Codemagic に登録** https://codemagic.io/apps → Body-log-App → ⚙️ → Environment variables
   - name `GCLOUD_SERVICE_ACCOUNT_CREDENTIALS` / value **JSONの中身を全部** / group `google_play`
   - **Secret にチェック**（Play への書き込み権限そのもの。JSONの中身はチャットに貼らない）

</details>

- サービスアカウント: `codemagic-play-publisher@bodyloger.iam.gserviceaccount.com`
  （Play の権限は「テスト版リリースを管理」＋「アプリ情報の閲覧」に限定・製品版の権限は無し）
- 製品版への昇格は自動化していない（審査に出る経路は必ず人間が踏む）
- 詳細と設計判断は docs/ANDROID.md「Playへの自動アップロード」

### A12. 🔴 Play: クローズドテスト（個人用アカウントは製品版の前に必須）

2026-09-04 に Play Console のダッシュボードで判明:

> 全ユーザーを対象に公開するには、アプリの設定を完了し、**クローズド テストを行ったうえで、
> 製品版へのアクセスを申請する必要があります**

このアカウントは**個人用アカウント**（アカウントID 8692850423678392643・2023年11月13日以降に
作成）なので、新規個人アカウント向け規定の対象。**内部テストから製品版へは直接行けない。**

### 条件（2026-09-04 に公式ヘルプで確認した確定値）

出典 https://support.google.com/googleplay/android-developer/answer/14151465

| 項目 | 条件 |
|---|---|
| テスター数 | **12人以上** |
| 期間 | **14日間連続でオプトインした状態** |
| 審査 | 申請後 **通常7日以内**（それ以上かかることもある） |

> a minimum of 12 testers who have been opted in continuously for at least 14 days

**14日は「テスター1人ごとの連続オプトイン期間」**であって、テスト全体の日数ではない:

> Testers who opt in, test for fewer than 14 days, and then opt out do not count
> toward the requirement.

つまり**途中でアンインストール（オプトアウト）した人はカウントから外れる**。
声をかけるときに「2週間は入れたままにしてほしい」を明示的に伝える必要がある。
12人を割ったら、その時点から積み直しになる。

申請フォームでは3つの節に答える。テスターが**全機能を使ったか**、
**本番のユーザー行動に近い使い方だったか**も問われるので、
「入れただけ」の12人では通らない可能性がある。

### 手順

1. **A5（アプリのコンテンツ申告）を完了する** ← これが無いとクローズドテストを公開できない
2. https://play.google.com/console → BodyLoger → **テストとリリース → テスト → クローズドテスト**
   → トラックを作成 → .aab を「ライブラリから追加」（再ビルド不要）→ 公開
3. **テスターを12人以上集める**。メールアドレスのリスト、または Google グループで管理
   - 現状は3名（本人・小澤祥太さん・KishimotoYumiさん）＋彩羽根田さん未承諾 → **あと9人以上**
   - オプトインURLからインストールし、**14日間入れたままにしてもらう**
4. 14日経過 → **製品版へのアクセスを申請**（ダッシュボードから・3節の質問に回答）
5. 審査（通常7日以内）→ 製品版公開

### 日程への影響

```
A5（コンテンツ申告）完了
  ↓
クローズドテスト公開 ＋ テスター12人がオプトイン
  ↓  14日間（1人でも抜けると、その人はカウント外）
製品版へのアクセスを申請
  ↓  通常7日以内（それ以上も）
製品版公開
```

**最短3週間・現実的には1か月弱。** iOS（審査だけ）とは別スケジュールになる。
「iOSと同時に出す」前提で計画していたなら、ここで見直す必要がある。

**14日のカウントはクローズドテストを公開してから始まるので、A5 が日程の律速。**
A5 を1日遅らせると、製品版公開が1日遅れる。

副産物: このクローズドテストは **A4（リリース前レポートでクラッシュのトレースを取る）**
も兼ねられる（リリース前レポートは内部テストでは生成されず、クローズドテスト以上で出る）。

---

## B. 開発側の残作業

### B1. 🔴 進行中: 広告の位置とインタースティシャル
- 食事タブのバナーを「あと食べられる」直下へ
- 概要のドリルダウン時に全画面広告（出す/出さないを詳細キーごとに制御）
- ブランチ `feat/ads-interstitial`

### B2. 🟠 マイ食品を常に全表示にする
現在は ▦/▬ で「1行スクロール ⇄ 全展開」を切替。**常に全部出す**方式へ。
- 切替ボタン・`foodsView` state・`bl-foods-view` を廃止
- **注意**: 1行モードにあった**ドラッグ並び替え**が失われる。管理シート（設定＞マイ食品）へ
  並び替えを移すか、割り切るかの判断が必要

### B3. 🟠 起動画面アイコンの四角が見える
アクア地の角丸タイルと `splash.backgroundColor` の境目が色差で見える。
- 実測: 四隅 `200,250,251` / タイル内側 `201,251,251` / 影のあたり `112,194,197`
- 皿だけを切り抜いて背景と完全に馴染ませる（グラデーションで境目を消す）

### B4. 🟡 Android 実機の全画面一巡
起動できた先の検証がまだ 0。docs/RELEASE-RISKS.md C3

### B5. 🟡 1.2.0 の主機能: カレンダー連携
設計完了（docs/CALENDAR.md・B-19）。**1.1.0 提出後に着手**と確定
- 端末内のみ・件名は保存も AI 送信もしない・既定 OFF・検出したら必ず確認
- 副産物: 会議中は記録リマインダーを 30 分ずらす

### B6. 🟡 ファネル計測基盤
1.1.0 提出後。独自の利用状況イベントはプライバシーラベル申告が増えるため提出直前は避ける。
App Store Connect の標準指標＋Vercel Analytics＋自前の最小イベント（docs/STRATEGY.md ⏸）

### B7. 🟢 体重グラフの共有ステッカー: 実数表示の既定
安全ガード G7（実数マスク）との衝突により、いまは「開始・現在の体重も載せる」チェックが
**既定 OFF**。常時表示にしたい場合はチェックを外すだけ（docs/FEATURES.md）

### B8. 🟢 Expo テンプレート残骸の掃除（済）→ 再発防止テストあり
`uiConvention.test.ts` が復活を検知して落とす

### B9. 🟡 1日の区切り（論理的な今日）をユーザー設定にする
**1.1.0 提出後・専用の設計セッションが必要**（提出直前に踏む橋ではない）。

- **何をしたいか**: 「今日」の切り替わりを JST 0:00 固定ではなく本人が決められるようにする（例 4:00）。
  深夜1時の夜食が「翌日の1食目」になる問題を、逃げ道ではなく**根本**で解決する
- **今回（2026-09-04・feat/wake-time）やらなかった理由**: `todayJST()` の意味を変えると波及範囲が広すぎる
  - `todayJST` の呼び出しが **38ファイル**（テスト除く。画面・通知・同期・特徴量・ウィジェット）
  - **保存済みデータの日付の意味が変わる**（＝過去の記録が別の日に移って見える）。
    `logs.date` / `entries.date` は DB の列そのもので、マイグレーションでは直せない（「本人がどう感じていたか」は残っていない）
  - ストリーク（連続記録）・週集計・月集計・週目標・バッジの達成日が全部ズレる
  - AsyncStorage のキー名（`bl-day-plan:<date>`・`bl-mood-snooze`・`bl-risk-snooze` 等）も日付を持っている
  - 代わりに **①「朝に出すものの時間の窓」（設定「起床時刻」）** と **②深夜の食事に「前日として記録」** の2つで
    体感のズレを埋めた（docs/FEATURES.md「起床時刻」節）
- **やるときの設計メモ**:
  - **既定は 4:00 が妥当**（一般的な「日付変更線」の慣習・24時間営業や飲食の締めの感覚と合う）。
    設定「起床時刻」とは**別の値**にすること（起床7:00の人の区切りが7:00だと、朝食前の記録が前日に入る）
  - `todayJST()` を直接呼ぶのをやめ、`logicalToday(now, cutoffHour)` 1本に寄せる（`uiConvention.test.ts` で直呼びを禁止する）
  - 既存データは**再解釈しない**（過去の `date` は当時の区切りで正しい）。切り替えた日を記録し、集計は
    「切替日より前は旧区切り」で読む…のか、割り切って「これから先だけ」にするのかを最初に決める
  - ストリーク・週集計・特徴量（`lib/features.ts`）の各所で「日付の境界」をどこから取るかを棚卸しする
  - ホームウィジェット（`setDayStatus`）とサーバー側（Vercel の集計・`lib/calc.ts`）の区切りも合わせる必要がある

---

## C. 完了（1行記録・2026-09-04）

- **Play への自動アップロード**（サービスアカウント＋`publishing > google_play`・track=internal）
- **AdMob インタースティシャルIDを Codemagic に登録**（iOS/Android 各1・実際の表示は A9 以降）
- **`ENABLE_ASSOCIATED_DOMAINS=true` を登録**（ログイン自動入力・iOS再ビルドで有効化）

- **Supabase migration 23〜32 を実行完了**（`migration-23-32-all.sql` で一括。27を含むので
  次回起動で全ユーザーに再同意画面が出る＝規約全面改訂に伴う正しい挙動）
- 1.1.0 を iOS（build 81/84）と Android（versionCode 119）で両方ビルド成功
- N1〜N4（今日の予定ヒアリング＋再配分・未来シミュレーション・司令塔・Weekly Review）
- 食材ナビ（栄養DB86品・スマート置き換え・栄養ランキング・たんぱく源ティア）
- インサイト・エンジン（日次特徴量・相関・法則17種・解説記事＋出典・気づきアラート）
- 目標統合／週間・月間収支／超過3段階／食事入力を＋ボタン2段→直行へ／記録行タイポ
- 筋トレ全画面（レスト・セット別回数・加重/補助ダイアル）／運動記録シート／並び替え
- 消費kcalが目標に反映されないバグ（BMR下限が運動ぶんを飲み込んでいた）
- ヘルスケア恒久同期／新パレット（AA検査）／広告全タブ＋課金即時消去／招待／生理周期
- フィードバック窓口＋★依頼／マイ食品AI登録／自己検閲（UX16件・バグ12件）
- テーマまだらバグの根治（参照の同一性）／日付跨ぎ／王冠の嘘／アイコン黒縁
- Android 署名（デバッグ鍵で署名されていた）＋ CI に署名検証を追加
- Android 起動を止めない構造（safeBoot）＋起動エラーの端末内記録＋Intl 依存の排除
- 「記録する」シートのコンパクト化（477pt → 371pt）
